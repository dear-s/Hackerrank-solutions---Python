def inOrder(root):
    #Write your code here

    if root is None:
        return
    
    #recursive method - easy 
    #Inorder traversal - left root right
    
    inOrder(root.left)
    
    print(root.info, end = " ")

    inOrder(root.right) 
